[project]
name = "nwb-api"
version = "1.0.0"
description = "NWB Workout API — FastAPI backend with GitHub OAuth and Neon Postgres"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
    "asyncpg>=0.30.0",
]

[tool.uv]
dev-dependencies = []
