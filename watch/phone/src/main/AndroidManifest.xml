<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".NwbPhoneApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NWB Workout"
        android:supportsRtl="true"
        android:theme="@style/Theme.NwbPhone">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="NWB Workout"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Wearable Data Layer listener for watch sync -->
        <service
            android:name="com.nwb.phone.data.sync.PhoneSyncManager"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/nwb/workout_sync"
                    android:scheme="wear" />
            </intent-filter>
        </service>
    </application>

</manifest>
