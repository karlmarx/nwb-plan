[project]
name = "nwb-plan-e2e"
version = "0.1.0"
description = "Playwright E2E tests for nwb-plan"
requires-python = ">=3.14"

[project.optional-dependencies]
test = [
    "playwright",
    "pytest",
    "pytest-playwright",
]

[tool.pytest.ini_options]
testpaths = ["e2e"]
